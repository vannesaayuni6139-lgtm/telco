// Centralized runtime configuration for the client app.
// Move values here so they can be easily discovered and changed.

// Admin email used by the demo client to detect admin users.
export const ADMIN_EMAIL = 'admin@telco.dev';
